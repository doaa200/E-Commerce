import { ProductcardDirective } from './productcard.directive';

describe('ProductcardDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductcardDirective();
    expect(directive).toBeTruthy();
  });
});
